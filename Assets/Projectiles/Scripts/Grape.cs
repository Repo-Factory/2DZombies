using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Grape : Projectile
{
    // Start is called before the first frame update
    void Start()
    {
        damage = 10.0f;
        speed = 1.0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
